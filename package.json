{
  "name": "geek-regime",
  "version": "1.0.0",
  "description": "A collective blog on geek-wise stuff",
  "author": "Alexey Soloviev",
  "scripts": {
    "start": "docker-compose -f docker-compose.yml -f docker-compose.production.yml up --detach",
    "dev": "docker-compose -f docker-compose.yml -f docker-compose.development.yml up",
    "down": "docker-compose -f docker-compose.yml -f docker-compose.production.yml down"
  },
  "license": "ISC",
  "dependencies": {}
}
