import clearRouteCache from "./clearRouteCache";
import clearUserlessCookie from "./clearUserlessCookie";
import handleError from "./handleError";
import isAdmin from "./isAdmin";
import isAuthed from "./isAuthed";
import logError from "./logError";
import readRouteCache from "./readRouteCache";
import writeRouteCache from "./writeRouteCache";

export default {
    clearRouteCache,
    clearUserlessCookie,
    handleError,
    isAdmin,
    isAuthed,
    logError,
    readRouteCache,
    writeRouteCache
};
